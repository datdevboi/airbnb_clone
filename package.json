{
  "private": true,
  "scripts": {
    "build:server": "lerna run build",
    "build:web": "lerna run build"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
