{
  "private": true,
  "scripts": {
    "build:server": "lerna run build",
    "build:web": "learn run build --scrope={@airbnbclone/common, @airbnbclone/web}"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/rimraf",
      "**/rimraf/**"
    ]
  },
  "devDependencies": {
    "lerna": "^2.11.0"
  }
}
